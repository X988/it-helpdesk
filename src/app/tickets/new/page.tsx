"use client";
import { FormEvent,useEffect,useState } from "react";import { useRouter } from "next/navigation";
type Category={id:string;name:string};
export default function NewTicket(){const router=useRouter();const [categories,setCategories]=useState<Category[]>([]);const [error,setError]=useState("");const [busy,setBusy]=useState(false);useEffect(()=>{fetch("/api/categories").then(r=>r.ok?r.json():Promise.reject()).then(d=>setCategories(d.categories)).catch(()=>setError("Не удалось загрузить категории"));},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=e.currentTarget;const f=new FormData(form);const r=await fetch("/api/tickets",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({subject:f.get("subject"),description:f.get("description"),categoryId:f.get("categoryId"),priority:f.get("priority")})});if(!r.ok){setError("Не удалось создать заявку");setBusy(false);return}const {ticket}=await r.json();const files=(form.elements.namedItem("files") as HTMLInputElement)?.files;if(files?.length){const body=new FormData();Array.from(files).forEach(file=>body.append("files",file));const u=await fetch(`/api/tickets/${ticket.id}/attachments`,{method:"POST",body});if(!u.ok)setError("Заявка создана, но часть файлов загрузить не удалось");}router.push(`/tickets/${ticket.id}`);router.refresh();}return <main className="shell"><div className="card formCard"><p className="muted">IT HELP DESK</p><h1>Новая заявка</h1><form onSubmit={submit} className="ticketForm"><label>Тема<input name="subject" required minLength={3} maxLength={160}/></label><label>Категория<select name="categoryId" required defaultValue=""><option value="" disabled>Выберите категорию</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Приоритет<select name="priority" defaultValue="NORMAL"><option value="LOW">Низкий</option><option value="NORMAL">Обычный</option><option value="HIGH">Высокий</option><option value="URGENT">Срочный</option></select></label><label>Описание<textarea name="description" required minLength={5} maxLength={10000} rows={8}/></label><label>Скриншоты и файлы<input name="files" type="file" multiple accept="image/png,image/jpeg,image/webp,application/pdf,text/plain"/></label><p className="muted">До 5 файлов, каждый до 10 МБ.</p>{error&&<p className="error">{error}</p>}<button disabled={busy}>{busy?"Создание…":"Создать заявку"}</button></form></div></main>}
