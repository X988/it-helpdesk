import Link from "next/link";import { notFound,redirect } from "next/navigation";import { db } from "@/lib/db";import { getSession } from "@/lib/auth";import { canReadTicket } from "@/lib/ticket-access";import TicketActions from "./TicketActions";
export default async function TicketPage({params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)redirect("/login");const {id}=await params;const t=await db.ticket.findUnique({where:{id},include:{category:true,requester:{select:{name:true,email:true,organization:true,department:true}},assignee:{select:{name:true}},attachments:true,messages:{where:s.role==="USER"?{visibility:"PUBLIC"}:{},include:{author:{select:{name:true,role:true}},},orderBy:{createdAt:"asc"}},statusHistory:{orderBy:{createdAt:"asc"}}}});if(!t||!canReadTicket(s.role,s.userId,t.requesterId))notFound();const manage=s.role!=="USER";return <main className="shell"><Link href="/dashboard" className="muted">← К заявкам</Link><header className="ticketHeader"><div><p className="muted">HD-{t.number} · {t.category.name}</p><h1>{t.subject}</h1></div><span className="status">{t.status}</span></header><div className="ticketLayout"><section><article className="card"><h2>Описание</h2><p className="pre">{t.description}</p></article><article className="card section"><h2>Переписка</h2>{t.messages.length===0?<p className="muted">Сообщений пока нет.</p>:t.messages.map(m=><div className={`message ${m.visibility==="INTERNAL"?"internal":""}`} key={m.id}><b>{m.author.name}</b><span className="muted"> {m.author.role} · {m.createdAt.toLocaleString("uk-UA")}</span>{m.visibility==="INTERNAL"&&<small> INTERNAL</small>}<p className="pre">{m.body}</p></div>)}<TicketActions id={t.id} canManage={manage}/></article></section><aside><div className="card"><h3>Детали</h3><p><b>Приоритет:</b> {t.priority}</p><p><b>Автор:</b> {t.requester.name}</p><p><b>Отдел:</b> {t.requester.department??"—"}</p><p><b>Организация:</b> {t.requester.organization??"—"}</p><p><b>Специалист:</b> {t.assignee?.name??"Не назначен"}</p></div><div className="card section"><h3>Файлы</h3>{t.attachments.length===0?<p className="muted">Нет файлов</p>:t.attachments.map(a=><p key={a.id}><a href={`/api/attachments/${a.id}`} target="_blank">{a.originalName}</a> <span className="muted">({Math.ceil(a.size/1024)} KB)</span></p>)}</div><div className="card section"><h3>История</h3>{t.statusHistory.map(h=><p key={h.id} className="muted">{h.fromStatus??"—"} → {h.toStatus}<br/>{h.createdAt.toLocaleString("uk-UA")}</p>)}</div></aside></div></main>}
