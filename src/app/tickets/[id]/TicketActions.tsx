"use client";
import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";
export default function TicketActions({id,canManage}:{id:string;canManage:boolean}){const router=useRouter();const [error,setError]=useState("");async function claim(){const r=await fetch(`/api/tickets/${id}/claim`,{method:"POST"});if(!r.ok)setError("Заявку уже взял другой специалист или действие недоступно");router.refresh();}async function status(value:string){const r=await fetch(`/api/tickets/${id}/status`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({status:value})});if(!r.ok)setError("Переход статуса недоступен");router.refresh();}async function message(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const r=await fetch(`/api/tickets/${id}/messages`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({body:f.get("body"),visibility:f.get("visibility")??"PUBLIC"})});if(r.ok){form.reset();router.refresh()}else setError("Сообщение не отправлено");}return <div className="actions"><div className="actionRow">{canManage&&<><button onClick={claim}>Взять в работу</button><button className="secondary" onClick={()=>status("WAITING_FOR_USER")}>Ждём пользователя</button><button className="secondary" onClick={()=>status("RESOLVED")}>Решено</button></>}</div><form onSubmit={message} className="ticketForm"><label>Сообщение<textarea name="body" required rows={4}/></label>{canManage&&<label>Видимость<select name="visibility" defaultValue="PUBLIC"><option value="PUBLIC">Публичное</option><option value="INTERNAL">Внутренняя заметка</option></select></label>}<button>Отправить</button></form>{error&&<p className="error">{error}</p>}</div>}
